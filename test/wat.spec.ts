import {assert} from 'chai';
import {$_$} from "../src/wat.js";

describe("Tests", () => {
    it('should run', () => {
        // Arrange
        // Act
        // Assert
        assert.equal($_$, $_$);
    });
});
